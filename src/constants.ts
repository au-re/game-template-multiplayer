// hack to keep font readable
export const scaleRatio = 4;

export const tickDuration = 160;

export const gameTitle = "Game Template";

export enum GameEvents {
  LOCAL_STATE_UPDATE = "LOCAL_STATE_UPDATE",
  BEAT_START_EVENT = "BEAT_START_EVENT",
  BEAT_END_EVENT = "BEAT_END_EVENT",
}
